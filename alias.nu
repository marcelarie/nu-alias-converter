# alias ls = ls --color=auto # Invalid nushell alias
alias ll = ls -l
alias abc! = echo String with special characters
# alias la = ls -A # Invalid nushell alias
alias gitlog = git log --graph --oneline --decorate --all
# alias invalid_nushell_alias = echo $HOME # Invalid nushell alias
# alias node15 = source /usr/share/nvm/init-nvm.sh # Invalid nushell alias
alias zkn = cd ~/notes; ~/scripts/zk-new
